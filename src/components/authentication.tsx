import Register from "./register";

const Auth: React.FC = () => {
  return (
    <div className="bg-slate-100 min-h-screen w-screen flex items-center justify-center">
      <Register />
    </div>
  );
};

export default Auth;
